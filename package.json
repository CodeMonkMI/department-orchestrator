{
  "name": "do",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "db:up": "docker-compose -f docker/docker-compose.yaml up -d"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}