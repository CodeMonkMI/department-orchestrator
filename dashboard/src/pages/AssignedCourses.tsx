import Layout from "@/components/layout/Layout";
import CourseHistoryContainer from "@/features/course/features/history/CourseHistory";

const AssignedCourses = () => {
  return (
    <Layout>
      <CourseHistoryContainer />
    </Layout>
  );
};

export default AssignedCourses;
