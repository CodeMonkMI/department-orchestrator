import SettingsLayout from "@/features/settings/components/Layout";
import NotificationsContainer from "@/features/settings/features/notifications/Notifications";

const Notifications = () => {
  return (
    <SettingsLayout>
      <NotificationsContainer />
    </SettingsLayout>
  );
};

export default Notifications;
