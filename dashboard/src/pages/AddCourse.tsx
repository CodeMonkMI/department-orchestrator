import Layout from "@/components/layout/Layout";
import AddNewCourseContainer from "@/features/course/features/add-new/add-new-course";

const AddCourse = () => {
  return (
    <Layout>
      <AddNewCourseContainer />
    </Layout>
  );
};

export default AddCourse;
